import { ScreenConfig } from "./repo/ScreenConfigs";
export declare const appWithScreenConfig: {
    screen(path: string, cb: (ctx: app.Ctx, config: ScreenConfig, req: app.Req) => any): {
        url(configId: string): string;
    };
};
