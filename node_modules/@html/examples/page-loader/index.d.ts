import { jsx } from '@app/html-jsx';
export declare const PageInitializer: {
    (props: {
        ref: string;
        ctx: import("@app/html-jsx").InitializerCtx;
    }): jsx.JSX.Element;
    initializer: (props: Omit<unknown, "ctx" | "element"> & {
        element: Element;
        ctx: {
            t: (input: string) => string;
            account: {
                url(path: string): string;
            };
        };
    }) => void;
};
