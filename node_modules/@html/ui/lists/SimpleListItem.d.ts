import { jsx } from '@app/html-jsx';
import type { Item } from './List';
export declare const SimpleListItem: (props: SimpleListItemProps) => jsx.JSX.Element;
export interface SimpleListItemProps {
    item: Item;
    selected?: boolean;
}
