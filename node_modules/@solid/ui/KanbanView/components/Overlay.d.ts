import { FlowComponent, JSXElement } from '@app/solid-js';
import { Draggable } from '../KanbanView';
interface OverlayProps {
    when: Draggable | null;
    children: JSXElement | ((activeDraggable: Draggable) => JSXElement);
}
export declare const Overlay: FlowComponent<OverlayProps>;
export {};
